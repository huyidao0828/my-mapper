<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mymapper.dao.TestsDao">
  <select id="searchAllbypage" resultType="hashmap" parameterType="integer">
    select *
    from tests
where age=#{age}
  </select>
</mapper>